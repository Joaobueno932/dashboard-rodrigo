# Set in your shell or process manager; this file is documentation, not auto-loaded.
HOST=127.0.0.1
PORT=8000
MAX_UPLOAD_MB=10
COOKIE_SECURE=false
# DATA_DIR=/absolute/persistent/directory
# ADMIN_PASSWORD_HASH=   # optional scrypt hash; prefer CLI configure
# DATABASE_URL=   # conexão do banco; valor real fica no .env, fora do Git
